apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-deploy
  namespace: openshift-gitops
  labels:
    argocd/app-name: buildah-demo-app
  annotations:
    argocd/app-name: buildah-demo-app
spec:
  destination:
    server: 'https://kubernetes.default.svc'
  project: default
  source:
    helm:
      parameters:
        - name: namespace
          value: <here-goes-which-namespace-will-use>
    path: ./deployment
    repoURL: 'https://github.com/sgutierr/buildah-demo.git'
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 10m
      limit: 10
    syncOptions:
      - CreateNamespace=true